import Received from '../../assets/icons/activity/received.svg';
import { createNativeIcon } from '../icon/create-icon.native';

export const ReceivedIcon = createNativeIcon({
  icon: {
    small: Received,
  },
  displayName: 'ReceivedIcon',
});
