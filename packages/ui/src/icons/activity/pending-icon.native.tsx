import Pending from '../../assets/icons/activity/pending.svg';
import { createNativeIcon } from '../icon/create-icon.native';

export const PendingIcon = createNativeIcon({
  icon: {
    small: Pending,
  },
  displayName: 'PendingIcon',
});
