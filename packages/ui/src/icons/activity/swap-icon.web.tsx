import Swap from '../../assets/icons/activity/swap.svg';
import { createWebIcon } from '../icon/create-icon.web';

export const SwapIcon = createWebIcon({
  icon: {
    large: Swap,
  },
  defaultVariant: 'large',
  displayName: 'SwapIcon',
});
