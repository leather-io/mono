import Failed from '../../assets/icons/activity/failed.svg';
import { createNativeIcon } from '../icon/create-icon.native';

export const FailedIcon = createNativeIcon({
  icon: {
    small: Failed,
  },
  displayName: 'FailedIcon',
});
