rules:
  unpinned-uses:
    config:
      policies:
        '*': hash-pin
  secrets-outside-env:
    config:
      allow:
        - ISSUE_PR_COMMENTS_BOT
        - CODECOV_TOKEN
        - CLAUDE_CODE_OAUTH_TOKEN
