rules:
  unpinned-uses:
    config:
      policies:
        '*': hash-pin
  secrets-outside-env:
    config:
      allow:
        - CODECOV_TOKEN
        - CLAUDE_CODE_OAUTH_TOKEN
        - SENTRY_AUTH_TOKEN
