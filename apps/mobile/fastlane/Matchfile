git_url("https://github.com/leather-io/mobile-signing-certificates")

storage_mode("git")

type("appstore") # The default type, can be: appstore, adhoc, enterprise or development

app_identifier(["io.leather.mobilewallet"])
