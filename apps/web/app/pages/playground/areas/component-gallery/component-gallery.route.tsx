import { WhenClient } from '~/components/when-client';

import { ComponentGalleryPage } from './component-gallery.page';

export default function ComponentGalleryRoute() {
  return (
    <WhenClient>
      <ComponentGalleryPage />
    </WhenClient>
  );
}
