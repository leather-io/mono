import { WhenClient } from '~/components/when-client';

import { PageGalleryPage } from './page-gallery.page';

export default function PageGalleryRoute() {
  return (
    <WhenClient>
      <PageGalleryPage />
    </WhenClient>
  );
}
